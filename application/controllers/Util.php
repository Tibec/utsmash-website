<?php

/**
 * Created by PhpStorm.
 * User: Benjamin
 * Date: 03/08/2016
 * Time: 19:16
 */
class Util extends BaseController
{
/*
    public function index()
    {
        $this->load->library('email');
        $this->email->set_newline("\r\n");
        $this->email->from("utsmash.asso@gmail.com", "UT'Smash");
        $this->email->to('benjamin.roelandt@gmail.com');
        $this->email->subject('Confirmation d\'inscription');
        $this->email->message("bzzzzz");
        if(!$this->email->send(false))
        {     echo        $this->email->print_debugger();  }
    }

    public function switchuser($uid)
    {
        // FOR DEBUG PURPOSE ONLY
        $this->session->user_id = $uid;
    }

    public function tournamentplayer($evId)
    {
        $this->load->model('Tournament_model', 'tournamentMgr');
        $p = $this->tournamentMgr->getPlayersForEvent($evId);
        foreach($p as $player)
        {
            echo $player->gametag.' | '.$player->pseudo;
        }
    }
*/

};
